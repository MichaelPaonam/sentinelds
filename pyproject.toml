[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sentinelds"
version = "0.1.0"
description = "An agentic data-science workspace defended by Dynatrace."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"